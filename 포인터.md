```C
int* cal();

void main() {
	int* b = cal();
	printf("%d", *b);
	free(b);
}

int* cal() {
	int c[] = { 1,4,6 };
	int* k = (int*)malloc(sizeof(int) * 3);
	for (int i = 0; i < 3; i++) {
		k[i] = c[i];
	}
	return k;
}
```